import {Input as NativeBaseInput} from 'native-base';

export function Input() {
  return(
   <NativeBaseInput bg="gray.700" h={14} size="md" fontFamily="body" color="white" placeholderTextColor="gray.300"/>
  );
}
